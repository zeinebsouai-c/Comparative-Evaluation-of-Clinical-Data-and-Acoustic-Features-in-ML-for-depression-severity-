# Versions the analysis was last run and verified with (Python 3.11).
# Install with: pip install -r requirements.txt
#
# Conda users: environment.yml pins the same set. Keep the two in sync.
# Not covered here: openSMILE 3.0, a standalone binary bundled under
# audio_lanzhou_2015/tools/ (or pointed at via the OPENSMILE_ROOT env var).

pandas>=2.2.2
numpy>=2.2.6
scipy>=1.17.1
scikit-learn>=1.9.0

# Excel metadata sheet (pandas.read_excel engine -- imported indirectly)
openpyxl>=3.1.5

# Plotting
matplotlib>=3.8.4
seaborn>=0.13.2

# Interpretability
shap>=0.51.0

# Audio loading / resampling / noise injection
librosa>=0.11.0
soundfile>=0.14.0

# Running the notebooks
jupyterlab>=4.2.0
notebook>=7.2.2
nbformat>=5.10.4
